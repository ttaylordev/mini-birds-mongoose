// make a birds Schema, but do not wrap it in a model
// the difference is that a Schema is a definition of the fields that are required
// the Model adds on all the ORM requests, all the methods.

// we can put a schema inside of a schema, we cannot put a model inside of a schema